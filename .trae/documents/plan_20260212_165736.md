## 问题分析
经过整体检查，代码功能基本完整，但存在一些小问题需要清理：

1. **未使用的变量**：
   - `selectedStarForSwap`变量（第463行）在新的操作逻辑中已经不再使用

2. **冗余代码**：
   - 双击处理函数中仍然有`selectedStarForSwap`的相关代码
   - 绘制函数中仍然有`selectedStarForSwap`的高亮显示代码

3. **代码清理**：
   - 移除未使用的变量和代码，提高代码可读性
   - 确保所有功能在手机端都能正常工作

## 解决方案

1. **删除未使用的变量**：
   - 移除`selectedStarForSwap`变量
   - 清理相关的引用代码

2. **优化代码结构**：
   - 清理双击处理函数，保留必要的备用功能
   - 简化绘制函数，移除未使用的高亮显示代码

3. **验证功能完整性**：
   - 确保棋子操作逻辑正常工作
   - 验证实时通信功能完整
   - 确保双人游戏功能正常

## 具体修改

1. **删除未使用的变量**：
   - 移除`selectedStarForSwap`变量定义
   - 清理绘制函数中的相关代码

2. **优化双击处理函数**：
   - 简化双击处理逻辑，只保留卫星翻面的备用功能

3. **验证功能**：
   - 确保棋子移动、翻面、换位等操作正常
   - 验证实时通信和双人游戏功能完整

这样修改后，代码将更加简洁，功能更加完整，同时确保在手机端也能正常工作。